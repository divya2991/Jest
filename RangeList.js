// The 'RangeList' class stores a list of ranges.
// A pair of integers define a range, for example: [1, 5). This range
//includes integers: 1, 2, 3, and 4.
// A range list is an aggregate of multiple ranges, for example: [1, 5),
//[10, 11), [100, 201)
class RangeList {
/**
* Adds a range to the list
* @param {Array<number>} range - Array of two integers that specify
beginning and end of the range.
*/
add(range) {
// Note: Implementation not provided.
}
/**
* Removes a range from the list
* @param {Array<number>} range - Array of two integers that specify
beginning and end of the range.
*/
remove(range) {
// Note: Implementation not provided.
}
/**
 * * Prints out the list of ranges in the range list
*/
print() {
    // Note: Implementation not provided.
    }
    }

module.exports = RangeList;